"""Custom reports — the composable report described in
`CUSTOM_REPORTS.md`: one metric × one dimension × typed filters,
resolved against the reporting layer (`v_fact_lines`, `v_dim_account`)
through a closed, enum-keyed allowlist. Read that file's Architecture
section before changing anything here."""
