"""Custom reports — the composable report of `SPEC.md` decision 25:
one metric × one dimension × typed filters, resolved against the
reporting layer (`v_fact_lines`, `v_dim_account`) through a closed,
enum-keyed allowlist. Read that decision before changing anything
here."""
