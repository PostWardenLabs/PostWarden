# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The intermediate OpenAPI JSON dump `npm run generate:api` writes before
# handing it to openapi-typescript (REBUILD_STATUS.md Phase 2.2) — purely
# derived from backend/'s own route declarations, not itself consumed by
# anything. src/api/schema.ts, what it's actually generated *into*, is
# committed (see src/api/client.ts's own comment on why).
openapi.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
