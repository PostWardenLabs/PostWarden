"""Settings — placeholder.

Real content (a pydantic-settings Settings class reading DATABASE_URL and
whatever else main.py's app factory needs) lands in Phase 1.2
(REBUILD_STATUS.md). Left empty deliberately in Phase 0: the directory shape
is what's being settled here, not the settings themselves.
"""
